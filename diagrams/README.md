# Architecture Diagrams

Drop your architecture diagram(s) here (e.g. `architecture.png`, `network-flow.png`)
and reference them from the root `README.md`. Since you already have a diagram,
place it in this folder as `architecture.png` and add:

```markdown
![Architecture](diagrams/architecture.png)
```

to the Architecture section of the root README.
